Commiter le premier dossier du pc à github.com

Ce placer à la racine des dossiers qu'on peut uploader sur github.com : D/B3/_Integration_Continu/tennis

-git init	sers à initialiser git dans le dossier
-git status	vérifier que la branche master y soit
-git add -A	ajouter tout ce qui se trouve dans le dossier
-git status	vérifier le commit initial à faire
-git commit -m "Premier commit"		commiter le contenu
-git remote add origin https://github.com/Alexis777/Tennis.git
-git pull origin master
-git push origin master
